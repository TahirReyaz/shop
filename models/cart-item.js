class CartItem {
  constructor(title, price, qty, sum, prodId) {
    this.title = title;
    this.price = price;
    this.qty = qty;
    this.sum = sum;
    this.prodId = prodId;
  }
}

export default CartItem;